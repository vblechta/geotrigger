SECRET_KEY=change-me-to-a-long-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
DATABASE_URL=sqlite:///geotrigger.db
TZ=Europe/Prague
# Bind address used by Docker / gunicorn, not Flask's debug server.
PORT=5000
